container_commands:
    01_pip:
        command: pip install --no-cache-dir -r requirements.txt --target $PYTHONPATH --force-reinstall
    02_migrate:
        command: python manage.py migrate informacion