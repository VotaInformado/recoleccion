container_commands:
    00_pip:
        command: pip uninstall -y -r requirements.txt --target $PYTHONPATH
    01_pip:
        command: pip install --no-cache-dir -r requirements.txt --target $PYTHONPATH
    02_migrate:
        command: python manage.py migrate informacion zero