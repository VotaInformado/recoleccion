version: '3'

services:
  django:
    build:
      context: .
      dockerfile: Dockerfile
    # image: andy912/vota-informado-recoleccion:latest
    # env_file:
    #   - .env # la crea elastic beanstalk
    ports:
      - "80:8000"


